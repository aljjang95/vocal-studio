# Administrator PWA candidate — 2026-09-17

Product: `aljjang95/vocal-studio`; baseline `add22e37f4ca3530b2f62170c75080e25cbda010`.
Goal: a home-screen administrator app for customers, lesson scheduling and payments, sharing the existing Cloudflare state across phone and desktop.

## Implemented
- Manifest explicitly preserves the previous same-origin start identity `/index.html`, uses standalone display, Korean admin labels, consistent theme colors and unrestricted orientation.
- Existing 192/512 PNG icons retain their actual sizes and declare `any`, not an unverified maskable safe area. Apple touch-icon and credentialed manifest loading are explicit.
- The active app has accessible native quick actions for today, schedule/registration and customer management, plus an expandable installation/use guide. The actual existing schedule engine and save path remain unchanged.
- Android/desktop browser-menu installation and iPhone/iPad Share → Add to Home Screen guidance are separate. A native install prompt is not simulated or automatically shown.
- Active startup no longer depends on an external Google font stylesheet; installed/system fallbacks remain.
- A real network failure while navigating to `/` or `/index.html` returns a generic 503 reconnect screen. It contains no customer data and uses no Cache Storage reads/writes. API errors, other paths, authentication responses and mutations are not replaced by that screen.
- No automatic page reload or background write queue was added. Existing saved local pending intent remains the sync controller's responsibility. Offline cold-start access to customer records is not provided.

## Verification and remaining gates
- Default Node suite: 85 passed, 0 failed, 0 skipped; eight new PWA tests.
- Actual local Worker + SQLite Durable Object + private R2 suite: 32 checks passed. Static/product gates and production-config dry-run passed.
- Extended the browser suite with manifest/installability, quick-action targets, installation-help layouts, service-worker offline navigation and reconnect checks.
- Fresh Chrome runs failed on initial `Page.goto` with a 30-second timeout, before those PWA/browser checks. No current PWA browser/OS-installation pass is claimed. The earlier 67-check result belongs to the prior source.
- One overlapping build encountered EBUSY; its error log is preserved. Later local runtime build passed. Removing the external font request did not eliminate the Chrome navigation timeout; its cause remains unconfirmed.
- An optional runtime helper append, independent review runner creation and alternate-browser diagnostic edit were blocked by tool security-state evaluation. The incomplete helper is outside the repository and not served. None of those blocked operations was rerouted or treated as completed.
- No production deployment, customer import/activation, Access or Firebase changes, data deletion, migration lock change or GitHub Actions creation occurred. This is not a production cutover.

Evidence: `C:\Users\Administrator\Documents\HANDOFF-2026-09-16\evidence\04-pwa-20260917`.
References: https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Guides/Making_PWAs_installable ; https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Manifest/Reference/id ; https://web.dev/articles/service-worker-lifecycle .
